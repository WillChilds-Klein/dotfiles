# TODO: automate vim set up
#  1. make sure vim is installed
#  2. make sure vundle is installed via GitHub (https://github.com/gmarik/Vundle.vim)
#    - into directory ~/.dotfiles/link/.vim/bundle/ or ~/.vim/bundle/
#  3. run shell command to install plugins:
#    $ vim +PluginInstall +qall

